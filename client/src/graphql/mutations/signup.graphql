mutation signUp($username: String!, $password: String!) {
  signUp(body: { username: $username, password: $password }) {
    errors {
      field
      message
    }
    user {
      ...userData
    }
  }
}
